# Use computer LAN IP for a Wi-Fi Android phone, not localhost.
EXPO_PUBLIC_BASE_URL=http://10.74.195.72:8000

# Android Google Maps SDK key (restrict to package + SHA fingerprints).
GOOGLE_MAPS_API_KEY=
